# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Test coverage
coverage/

# API key image
api-key.png
claude_session.txt
claude.txt
ziggy_tijd_testen.txt
SECURITY-TODO.md
infra/main.json

# Operational snapshots — local-only Cosmos dumps from scripts/backup-all.mjs.
# Contain live records and Cosmos metadata; keep them on the operator's
# machine, not in the public repo.
backups/*.json

# Local brainstorm/design notes — kept out of the public repo per project policy.
BRAINSTORM-*.md
.scratch/

# One-off ops/cleanup scripts (orphan blob audit, ad-hoc data-fix scripts).
# Live-data scripts that should never be in the public repo.
orphan-check.mjs
ops-*.mjs
